import { cn } from '@/lib/utils';

export default function SmartgridPhFigure() {
  return (
    <div
      className={cn(
        'w-full aspect-square place-content-center flex items-center justify-end overscroll-none bg-red-500/20'
      )}
    ></div>
  );
}
